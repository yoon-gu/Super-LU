sudo: true

language: cpp

compiler:
  - gcc
  - clang

os:
  - linux
  - osx

# command to run tests
script: 
  - export ROOT=`pwd`
  - make
  - make testing
  
after_success:
  - bash <(curl -s https://codecov.io/bash)